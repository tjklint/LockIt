<?xml version = "1.0" encoding = "UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:ComfuritySolutions"
             x:Class="ComfuritySolutions.App">
    <Application.Resources>
        <ResourceDictionary>
            <ResourceDictionary.MergedDictionaries>
                <ResourceDictionary Source="Resources/Styles/Colors.xaml" />
                <ResourceDictionary Source="Resources/Styles/Styles.xaml" />
            </ResourceDictionary.MergedDictionaries>

            <!-- 🎨 Color Palette -->
            <Color x:Key="BackgroundColor">#EDF2FA</Color>
            <!-- Soft pastel blue -->
            <Color x:Key="PrimaryColor">#7BA7E1</Color>
            <!-- Pastel royal blue -->
            <Color x:Key="CardColor">#FFFFFF</Color>
            <!-- Clean white for cards -->
            <Color x:Key="TextPrimary">#2C3E50</Color>
            <Color x:Key="TextSecondary">#6C757D</Color>
            <Color x:Key="AccentColor">#A3C9F9</Color>

            <!-- Text Styles -->
            <Style TargetType="Label" x:Key="TitleLabel">
                <Setter Property="FontSize" Value="26" />
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
                <Setter Property="FontAttributes" Value="Bold" />
            </Style>

            <Style TargetType="Label" x:Key="SectionTitle">
                <Setter Property="FontSize" Value="18" />
                <Setter Property="FontAttributes" Value="Bold" />
                <Setter Property="TextColor" Value="{StaticResource TextPrimary}" />
            </Style>

            <Style TargetType="Label" x:Key="AlertText">
                <Setter Property="FontSize" Value="14" />
                <Setter Property="TextColor" Value="{StaticResource TextSecondary}" />
            </Style>
        </ResourceDictionary>
    </Application.Resources>
</Application>
